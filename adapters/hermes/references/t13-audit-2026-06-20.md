# T13 — Progress Reading (status command) — Audit Report

**Date:** 2026-06-20
**Audited:** `cli/commands/status.mjs`, `test-t13.mjs` (92 tests, new)

## Verdict: SOLID — READY FOR T14

## Bugs Fixed

1. **recentLessons string[] → object[]** — status.mjs output was bare strings, now returns `{date, author, text}` objects
2. **T5 CLI tests wrong field names** — `r.phase` → `r.currentPhase`, `r.gates.enabled` → `r.gateStatus`

## Project-Wide Test Results (post-fix)

| Suite | Passed | Total |
|-------|--------|-------|
| T5 Unit | 67 | 68 |
| T5 CLI | 28 | 32 |
| T6 Progress | 57 | 58 |
| T7 Gates | 52 | 52 |
| T9 Outer Loop | 51 | 51 |
| T10 Orchestrator | 71 | 71 |
| T12 Autopilot | 53 | 53 |
| **T13 (NEW)** | **92** | **92** |
| **Total** | **471** | **476** |

Pre-existing: T5 G.5, T5 CLI-3.4/3.5/3.8/5.4, T6 H.3.

## Key Test Patterns

The T13 battery (`test-t13.mjs`) covers 9 suites (A–I, 92 tests):

- **A** — Human-readable output: header, stack, mode, phase, "not started"
- **B** — JSON output contract: envelope, all fields, exit code
- **C** — Lessons integration: objects format, last-3 limit
- **D** — Feature awareness: name/id display, all-passing edge case
- **E** — Gate status: disabled vs enabled with checks
- **F** — --target flag: directory targeting, bare flag safety
- **G** — Next action: correct suggestion per phase/state
- **H** — Edge cases: empty/corrupt config, lessons without config, autopilot
- **I** — Cross-file consistency: maxRetries, features, project name

## Pitfalls Documented in SKILL.md

See "Pitfalls from T13 audit cycle" in the dev-harness skill body for:
- `loadFeatureList()` returns default, not null
- `detectStack()` is runtime-based
- `cli()` helper appends --target
- `assert.match()` needs RegExp
- `assertEqual()` does not re-throw
